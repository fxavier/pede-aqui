package com.delivery.support.entity;

/** Incident classification categories used by support and admin users. */
public enum IncidentClassification {
    GENERAL,
    PAYMENT,
    DELIVERY,
    VENDOR,
    COURIER,
    FRAUD,
    CRITICAL
}
