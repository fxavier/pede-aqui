package com.delivery.marketing.entity;

/** Discount computation strategy for a promotion: percentage of the eligible base or a fixed MZN amount. */
public enum PromotionType {
    PERCENTAGE,
    FIXED_AMOUNT
}
